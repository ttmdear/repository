package com.repository.java.examples.illegaldev.objectstate;

public class CreateUserHandler {

    public void handle(CreateUser command) {
        // User user = new User(command.login, command.email);
        // userRepository.sava(user)
    }
}
